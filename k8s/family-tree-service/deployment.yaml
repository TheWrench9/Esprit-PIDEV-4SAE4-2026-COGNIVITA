apiVersion: apps/v1
kind: Deployment
metadata:
  name: family-tree-service
  namespace: cognivita
spec:
  replicas: 1
  selector:
    matchLabels:
      app: family-tree-service
  template:
    metadata:
      labels:
        app: family-tree-service
    spec:
      containers:
        - name: family-tree-service
          image: monta2027/family-tree-service:latest
          ports:
            - containerPort: 8083
          env:
            - name: DB_HOST
              value: "mysql-medical"
            - name: DB_PORT
              value: "3306"
            - name: DB_NAME
              value: "alzheimer_familytree"
            - name: DB_USER
              value: "root"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: medical-records-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: EUREKA_HOST
              value: "eureka-server"
            - name: MEDICAL_RECORDS_URL
              value: "http://medical-records-service:8081"
